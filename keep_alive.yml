name: Keep Alive Render
on:
  schedule:
    - cron: '*/14 * * * *' # Runs every 14 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Site
        run: curl -s https://service-story-generator.onrender.com/ > /dev/null
